package com.sandolpin.sdlrcmaker2.ui.home

import androidx.lifecycle.ViewModel

/**
 * ホーム画面用ViewModel。
 * 現状は各ボタンの遷移先を決めるだけの薄い画面のため状態は持たないが、
 * 将来的な「最近の下書きプレビュー表示」などの拡張に備えて用意している。
 */
class HomeViewModel : ViewModel()
